# Data, rendering and analysis (CPU only)
overcooked-ai
opencv-python-headless
pygame
pandas>=2.0          # the legacy-pickle shim in data_loader handles the 1.x -> 2.x break
numpy
pillow               # demo assets only

# Video LLM (needs a CUDA GPU; ~10 GB free for the 7B in 4-bit)
torch
transformers>=4.51
accelerate
qwen-vl-utils[decord]
bitsandbytes         # 4-bit NF4; drop it and pass --bf16 if you have ~17 GB free

# Tests
pytest
